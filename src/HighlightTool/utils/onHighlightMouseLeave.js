import $ from "jquery";

const onHighlightMouseLeave = (e) => {
  const hoverToolEl = $("#notealy-highlight-hover-tool-iframe-container");

  hoverToolEl.hide();
};

export default onHighlightMouseLeave;
